# Blocked items

## Chrome extension authorization required — 2026-08-09

The private site, loopback bridge, LaunchAgents, DeepSeek state machine, and five distinct real WeChat jobs are ready. The jobs remain `queued`; no article body has been claimed or generated.

Completion requires the user to explicitly approve loading the unpacked local extension and storing its loopback pairing token. The extension receives persistent read access only to `mp.weixin.qq.com` and sends extracted visible article content only to `127.0.0.1`. This confirmation was requested across three consecutive goal turns and has not yet been provided.

Resume action: user replies `允许安装`; then load `/Users/bytedance/Documents/思考/web/capture-bridge-extension` in Chrome, pair it, and run the five-link end-to-end validation.

## GitHub CLI preflight — 2026-08-03

`gh --version` exited 127 with `/bin/bash: gh: command not found`.

The public repository was created through the authenticated GitHub browser
session instead. Native Git push is still attempted below; no GitHub CLI-based
PR workflow is claimed.

## Private Sites deployment attempt 1 — 2026-08-03

Saved version 1 failed only at production certificate provisioning:
`Timed out waiting for the TLS certificate for thinking-intelligence-alpha.zyxelient.chatgpt.site`.

The build, source push, D1 migration archive, and owner-only access-policy checks completed before this platform failure. Two deployment retries remain before stopping.
